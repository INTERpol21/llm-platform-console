{
  "properties": {
    "backend": { "type": "string", "title": "Backend" },
    "documents": { "type": "integer", "title": "Documents" },
    "chunks": { "type": "integer", "title": "Chunks" },
    "embeddings_backend": { "type": "string", "title": "Embeddings Backend" },
    "llm_backend": { "type": "string", "title": "Llm Backend" },
    "embedding_dim": { "type": "integer", "title": "Embedding Dim" },
    "search_mode": { "type": "string", "title": "Search Mode" },
    "reranker": { "type": "string", "title": "Reranker" }
  },
  "type": "object",
  "required": [
    "backend",
    "documents",
    "chunks",
    "embeddings_backend",
    "llm_backend",
    "embedding_dim",
    "search_mode",
    "reranker"
  ],
  "title": "StatsResponse",
  "description": "Store counts plus the active backends (see the ``/stats`` route).",
  "x-readme-ref-name": "StatsResponse"
}
