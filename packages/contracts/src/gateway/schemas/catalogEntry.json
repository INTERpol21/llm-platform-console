{
  "properties": {
    "id": { "type": "string", "title": "Id" },
    "provider": { "type": "string", "title": "Provider" },
    "status": { "type": "string", "title": "Status" },
    "latency_ms": { "anyOf": [{ "type": "number" }, { "type": "null" }], "title": "Latency Ms" },
    "pricing": {
      "properties": {
        "input_per_1k": { "type": "number", "title": "Input Per 1K" },
        "output_per_1k": { "type": "number", "title": "Output Per 1K" }
      },
      "type": "object",
      "required": ["input_per_1k", "output_per_1k"],
      "title": "ModelPricing",
      "description": "USD cost per 1k tokens for a model.",
      "x-readme-ref-name": "ModelPricing"
    },
    "context_window": {
      "anyOf": [{ "type": "integer" }, { "type": "null" }],
      "title": "Context Window"
    },
    "fallbacks": { "items": { "type": "string" }, "type": "array", "title": "Fallbacks" },
    "streaming": { "type": "boolean", "title": "Streaming", "default": true }
  },
  "type": "object",
  "required": ["id", "provider", "status", "pricing"],
  "title": "CatalogEntry",
  "description": "Rich metadata for one aliased model (beyond the OpenAI /v1/models shape).",
  "x-readme-ref-name": "CatalogEntry"
}
